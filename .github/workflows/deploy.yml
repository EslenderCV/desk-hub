name: Deploy Desk Hub Stack

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: [self-hosted, linux, arm64]

    steps:
      - name: Checkout repository code
        uses: actions/checkout@v4

      - name: Rebuild and deploy Docker stack
        run: |
          cd infrastructure
          docker compose up -d --build